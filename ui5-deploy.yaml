# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "3.1"
metadata:
  name: com.yedas.mm.employees
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          url: http://10.249.21.27:8000/
          client: '100'
        app:
          name: ZEGT_FIORI_001
          description: Eğitim Fiori Uygulaması
          package: ZEGT
          transport: DWDK900684
        exclude:
          - /test/
